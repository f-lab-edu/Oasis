<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flab.oasis.mapper.UserCategoryMapper">

    <select id="findUserCategoryByUid" parameterType="java.lang.String" resultType="com.flab.oasis.model.UserCategory">
        SELECT UID, CATEGORY_ID AS categoryId
        FROM USER_CATEGORY
        WHERE UID=#{uid}
    </select>

</mapper>